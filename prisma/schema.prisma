generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema", "partialIndexes"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["catalogos", "core", "corpus", "graph", "params", "pendiente", "score", "security", "views", "z_borrame"]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model framework_doc {
  id                 BigInt   @id @default(autoincrement())
  object_schema      String
  object_name        String
  object_type        String
  object_subtype     String?
  parent_schema      String?
  parent_name        String?
  parent_type        String?
  column_name        String?
  ordinal_position   Int?
  data_type          String?
  udt_schema         String?
  udt_name           String?
  is_nullable        Boolean?
  column_default     String?
  object_description String?
  source_oid         Int?     @db.Oid
  is_active          Boolean  @default(true)
  discovered_at      DateTime @default(now()) @db.Timestamptz(6)
  refreshed_at       DateTime @default(now()) @db.Timestamptz(6)

  @@index([object_schema], map: "ix_framework_doc_object_schema")
  @@index([object_type], map: "ix_framework_doc_object_type")
  @@index([parent_schema, parent_name, parent_type], map: "ix_framework_doc_parent")
  @@schema("views")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_elements_control {
  control_id             String   @db.Uuid
  element_id             String   @db.Uuid
  satisfaction_mode      String
  evidence_required      Boolean  @default(true)
  min_coverage_threshold Decimal? @db.Decimal(5, 4)
  regulator_acceptance   String
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  updated_at             DateTime @default(now()) @db.Timestamptz(6)
  max_usable_coverage    Decimal  @db.Decimal(5, 4)
  role_in_satisfaction   String?
  is_primary             Boolean  @default(false)
  is_mandatory           Boolean  @default(true)
  aggregation_group      String?
  sequence_order         Int?     @db.SmallInt
  dependency_rule        String?
  coverage_weight        Decimal? @db.Decimal(6, 4)
  rationale              Json?

  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_elements_risk {
  element_id    String  @db.Uuid
  risk_id       String  @db.Uuid
  link_strength Int
  rationale     String?

  @@ignore
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_risk_control {
  control_id           String  @db.Uuid
  risk_id              String  @db.Uuid
  mitigation_strength  Int     @db.SmallInt
  framework_version_id String  @db.Uuid
  effect_type          String
  rationale            String?
  coverage_notes       String?

  @@ignore
  @@schema("core")
}

model security_rbac {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code        String   @unique
  name        String
  description String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("role")
  @@schema("security")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model security_user_scope {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id              String   @db.Uuid
  jurisdiction_id      String?  @db.Uuid
  framework_version_id String?  @db.Uuid
  domain_id            String?  @db.Uuid
  is_allowed           Boolean  @default(true)
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  created_by           String?  @db.Uuid

  @@ignore
  @@schema("security")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model security_user_token {
  id         String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String    @db.Uuid
  token_hash String
  token_type String
  expires_at DateTime  @db.Timestamptz(6)
  used_at    DateTime? @db.Timestamptz(6)
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  created_by String?   @db.Uuid

  @@ignore
  @@schema("security")
}

model security_users {
  id                          String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email                       String                       @unique @db.VarChar(255)
  password_hash               String
  name                        String?                      @db.VarChar(255)
  last_name                   String?
  whatsapp                    String?
  is_active                   Boolean?                     @default(true)
  must_change_password        Boolean                      @default(true)
  activation_status           String                       @default("pending")
  role_id                     String?                      @db.Uuid
  created_at                  DateTime?                    @default(now()) @db.Timestamp(6)
  updated_at                  DateTime?                    @default(now()) @db.Timestamp(6)
  last_login_at               DateTime?                    @db.Timestamptz(6)
  password_updated_at         DateTime?                    @db.Timestamptz(6)
  company_id                  String?                      @db.Uuid
  risk_treatment_responsibles risk_treatment_responsible[]
  map_users_x_roles           map_users_x_roles[]
  company                     borrame_company?             @relation(fields: [company_id], references: [id], map: "fk_security_users_company_id")

  @@schema("security")
}

model security_module {
  code        String   @id
  name        String
  description String?
  is_core     Boolean  @default(false)
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("module")
  @@schema("security")
}

model security_permission {
  code        String   @id
  module_code String
  name        String
  description String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("permission")
  @@schema("security")
}

model security_role_permission {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  role_id         String   @db.Uuid
  permission_code String
  is_active       Boolean  @default(true)
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@unique([role_id, permission_code])
  @@map("role_permission")
  @@schema("security")
}

model security_company_module {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id     String    @db.Uuid
  module_code    String
  is_enabled     Boolean   @default(true)
  license_status String    @default("active")
  starts_at      DateTime? @db.Timestamptz(6)
  ends_at        DateTime? @db.Timestamptz(6)
  metadata       Json      @default("{}")
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)

  @@unique([company_id, module_code])
  @@map("company_module")
  @@schema("security")
}

model security_company_license {
  id                String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id        String          @db.Uuid
  module_code       String
  license_payload   Json
  license_signature String
  issued_at         DateTime        @default(now()) @db.Timestamptz(6)
  expires_at        DateTime?       @db.Timestamptz(6)
  status            String          @default("active")
  created_at        DateTime        @default(now()) @db.Timestamptz(6)
  updated_at        DateTime        @default(now()) @db.Timestamptz(6)
  company           borrame_company @relation(fields: [company_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([company_id, module_code], map: "uq_company_license_company_module")
  @@map("company_license")
  @@schema("security")
}

model security_company_user_role {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id String   @db.Uuid
  user_id    String   @db.Uuid
  role_id    String   @db.Uuid
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([company_id, user_id, role_id])
  @@map("company_user_role")
  @@schema("security")
}

model security_license_audit_log {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id     String   @db.Uuid
  module_code    String
  license_id     String?  @db.Uuid
  action         String
  previous_state Json?
  new_state      Json?
  performed_by   String?  @db.Uuid
  source         String   @default("system")
  reason         String?
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@index([company_id], map: "idx_license_audit_company")
  @@index([created_at(sort: Desc)], map: "idx_license_audit_created_at")
  @@index([license_id], map: "idx_license_audit_license")
  @@index([module_code], map: "idx_license_audit_module")
  @@index([performed_by], map: "idx_license_audit_performed_by")
  @@map("license_audit_log")
  @@schema("security")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model run {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id           String   @db.Uuid
  framework_version_id String   @db.Uuid
  period_start         DateTime @db.Date
  period_end           DateTime @db.Date
  mode                 String
  status               String   @default("active")
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  updated_at           DateTime @default(now()) @db.Timestamptz(6)

  @@ignore
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model score_test_result_draft {
  run_id              String   @db.Uuid
  control_id          String   @db.Uuid
  dimension           String
  test_id             String   @db.Uuid
  score               Decimal  @db.Decimal(5, 4)
  passed              Boolean  @default(false)
  evidence_required   Boolean  @default(true)
  evidence_sufficient Boolean?
  key_test            Boolean  @default(false)
  assessment_method   String?
  evaluator_notes     String?
  reasons             Json     @default("[]")
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@ignore
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model score_test_result {
  run_id              String   @db.Uuid
  control_id          String   @db.Uuid
  dimension           String
  test_id             String   @db.Uuid
  score               Decimal  @db.Decimal(5, 4)
  passed              Boolean  @default(false)
  evidence_required   Boolean  @default(true)
  evidence_sufficient Boolean?
  key_test            Boolean  @default(false)
  assessment_method   String?
  evaluator_notes     String?
  reasons             Json     @default("[]")
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  updated_at          DateTime @default(now()) @db.Timestamptz(6)

  @@ignore
  @@schema("core")
}

model objective {
  objective_id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id            String   @default(dbgenerated("'00000000-0000-0000-0000-000000000000'::uuid")) @db.Uuid
  objective_code        String   @db.VarChar(100)
  objective_name        String   @db.VarChar(255)
  objective_description String?
  rationale             Json?
  sequence_order        Int?
  is_active             Boolean  @default(true)
  created_at            DateTime @default(now()) @db.Timestamptz(6)
  updated_at            DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

model risk_treatment {
  id                     String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  risk_id                String                       @db.Uuid
  title                  String                       @db.VarChar(255)
  treatment_type         String                       @db.VarChar(50)
  status                 String                       @db.VarChar(50)
  justification          String?
  description            String?
  decision_date          DateTime?                    @db.Date
  planned_start_date     DateTime?                    @db.Date
  planned_end_date       DateTime?                    @db.Date
  next_review_date       DateTime?                    @db.Date
  residual_risk_expected String?                      @db.VarChar(50)
  notes                  String?
  created_at             DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                    @default(now()) @db.Timestamptz(6)
  risk                   risks                        @relation(fields: [risk_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  actions                risk_treatment_action[]
  evidences              risk_treatment_evidence[]
  responsibles           risk_treatment_responsible[]

  @@schema("core")
}

model risk_treatment_action {
  id           String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  treatment_id String                       @db.Uuid
  description  String
  status       String                       @db.VarChar(50)
  due_date     DateTime?                    @db.Date
  created_at   DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at   DateTime?                    @default(now()) @db.Timestamptz(6)
  treatment    risk_treatment               @relation(fields: [treatment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  evidences    risk_treatment_evidence[]
  responsibles risk_treatment_responsible[]

  @@schema("core")
}

model risk_treatment_responsible {
  id                String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  treatment_id      String?                @db.Uuid
  action_id         String?                @db.Uuid
  responsible_id    String                 @db.Uuid
  responsible_name  String                 @db.VarChar(255)
  responsible_email String                 @db.VarChar(255)
  responsible_area  String?                @db.VarChar(100)
  responsible_role  String?                @db.VarChar(100)
  assigned_at       DateTime?              @default(now()) @db.Timestamptz(6)
  due_date          DateTime?              @db.Date
  is_primary        Boolean?               @default(false)
  is_active         Boolean?               @default(true)
  created_at        DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?              @default(now()) @db.Timestamptz(6)
  action            risk_treatment_action? @relation(fields: [action_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user              security_users         @relation(fields: [responsible_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  treatment         risk_treatment?        @relation(fields: [treatment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("core")
}

model risk_treatment_evidence {
  id            String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  treatment_id  String?                @db.Uuid
  action_id     String?                @db.Uuid
  filename      String                 @db.VarChar(255)
  original_name String                 @db.VarChar(255)
  file_path     String
  file_size     Int?
  mime_type     String?                @db.VarChar(100)
  uploaded_at   DateTime?              @default(now()) @db.Timestamptz(6)
  created_at    DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?              @default(now()) @db.Timestamptz(6)
  action        risk_treatment_action? @relation(fields: [action_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  treatment     risk_treatment?        @relation(fields: [treatment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model controls {
  id                 String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code               String
  name               String
  control_type_id    Int      @db.SmallInt
  automation_id      Int      @db.SmallInt
  frequency_id       Int      @db.SmallInt
  description        String
  owner_role         String?
  evidence_required  Boolean  @default(true)
  status             String   @default("active")
  status_id          Int      @default(2) @db.SmallInt
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  updated_at         DateTime @default(now()) @db.Timestamptz(6)
  control_objective  String?
  control_scope      String?
  is_hard_gate       Boolean  @default(false)
  rationale          Json     @default("{}")
  primary_cf_risk_id String?  @db.Uuid
  required_test      Boolean  @default(true)
  reino_id           String   @db.Uuid
  codigo_reino       String?

  @@map("_controls")
  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model controls_evaluation {
  id                   String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  control_id           String    @db.Uuid
  dimension_id         String    @db.Uuid
  code                 String
  title                String
  evaluation_guidance  String
  pass_criteria        String
  fail_criteria        String
  evaluator_steps      Json
  evidence_required    String
  evidence_min_spec    String?
  sample_guidance      String?
  notes                String?
  status               String    @default("draft")
  is_active            Boolean   @default(true)
  framework_version_id String?   @db.Uuid
  effective_from       DateTime  @default(now()) @db.Timestamptz(6)
  effective_to         DateTime? @db.Timestamptz(6)
  created_at           DateTime  @default(now()) @db.Timestamptz(6)
  updated_at           DateTime  @default(now()) @db.Timestamptz(6)

  @@map("_controls_evaluation")
  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model domains {
  id                         String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  framework_version_id       String   @db.Uuid
  code                       String
  name                       String
  description                String?
  status                     String   @default("active")
  created_at                 DateTime @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime @default(now()) @db.Timestamptz(6)
  domain_type_id             String?  @db.Uuid
  risk_scope_id              String?  @db.Uuid
  supervisory_relevance_id   String?  @db.Uuid
  weight_factor_id           String?  @db.Uuid
  applicability_rule_id      String?  @db.Uuid
  inherent_risk_relevance    Boolean?
  risk_management_relevance  Boolean?
  emerging_risk_flag         Boolean?
  requires_specialist_review Boolean?
  mtrix_status_id            String?  @db.Uuid
  codigo_reino               String?
  lider_id                   String?  @db.Uuid
  process_id                 String?  @db.Uuid
  domain_category            BigInt?

  @@index([lider_id], map: "idx_core_domain_lider_id")
  @@index([process_id], map: "idx_core_domain_process_id")
  @@map("_domains")
  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model elements {
  id                           String   @id(map: "domain_elements_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  element_type                 String
  code                         String
  name                         String?
  title                        String?
  description                  String?
  statement                    String?
  rationale                    Json?
  is_active                    Boolean  @default(true)
  is_hard_gate                 Boolean?
  criticality                  Int?     @db.SmallInt
  evidence_strength            Int?     @db.SmallInt
  criticality_id               Int?     @db.SmallInt
  evidence_strength_id         Int?     @db.SmallInt
  evaluation_focus             String?
  expected_evidence            String?
  risk_implication             String?
  inherent_risk_impact         Boolean?
  control_effectiveness_impact Boolean?
  sequence_order               Int?
  created_at                   DateTime @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime @default(now()) @db.Timestamptz(6)
  source_table                 String?
  source_pk                    String?  @db.Uuid
  codigo_reino                 String?
  id_lider                     String?  @db.Uuid
  id_frequency                 String?  @db.Uuid

  @@map("_elements")
  @@schema("core")
}

model kingdom {
  id          String   @id(map: "_1kingdom_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code        String   @unique(map: "_1kingdom_code_key") @db.VarChar(50)
  name        String   @db.VarChar(200)
  legal_name  String?  @db.VarChar(250)
  description String?
  status      Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("_kingdom")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risks {
  id                                  String           @unique(map: "core_risk_id_unique") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                                String
  name                                String
  risk_type                           String
  description                         String?
  created_at                          DateTime         @default(now()) @db.Timestamptz(6)
  updated_at                          DateTime         @default(now()) @db.Timestamptz(6)
  risk_type_id                        Int?             @db.SmallInt
  risk_layer_id                       Int              @default(2) @db.SmallInt
  parent_risk_id                      String?          @db.Uuid
  risk_origen                         String
  codigo_reino                        String?
  is_active                           Boolean          @default(true)
  risk_emerging_source_id             BigInt?
  risk_emerging_status_id             BigInt?
  risk_factor_id                      BigInt?
  operational_risk_loss_event_type_id BigInt?
  catalog_impact_id                   BigInt?
  catalog_probability_id              BigInt?
  risk_treatments                     risk_treatment[]

  @@index([is_active], map: "idx_core_risk_is_active")
  @@index([operational_risk_loss_event_type_id], map: "idx_risk_operational_risk_loss_event_type_id")
  @@index([risk_emerging_source_id], map: "idx_risk_risk_emerging_source_id")
  @@index([risk_emerging_status_id], map: "idx_risk_risk_emerging_status_id")
  @@index([risk_factor_id], map: "idx_risk_risk_factor_id")
  @@map("_risks")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model catalog_impact {
  catalog_impact_id BigInt   @id @default(autoincrement())
  code              String   @unique @db.VarChar(50)
  name              String   @db.VarChar(100)
  description       String?
  ordinal           Int      @unique
  numeric_value     Decimal? @db.Decimal(10, 4)
  is_active         Boolean  @default(true)
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model catalog_impact_guia {
  level        String
  description  String
  impact_score Int
  categoria    String?

  @@ignore
  @@schema("core")
}

model catalog_item {
  id               BigInt   @id @default(autoincrement())
  catalog_group_id BigInt
  realm_id         String   @db.Uuid
  code             String   @db.VarChar(100)
  name             String?  @db.VarChar(150)
  description      String?
  parent_id        BigInt?
  status_code      String   @default("ACTIVE") @db.VarChar(30)
  sort_order       Int      @default(0)
  ordinal_value    Int?
  numeric_value    Decimal? @db.Decimal(12, 4)
  direction_value  Int?
  metadata         Json     @default("{}")
  is_active        Boolean  @default(true)
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@unique([catalog_group_id, realm_id, code], map: "uq_catalog_item_group_realm_code")
  @@index([is_active], map: "idx_catalog_item_active")
  @@index([catalog_group_id], map: "idx_catalog_item_group")
  @@index([realm_id], map: "idx_catalog_item_realm")
  @@index([status_code], map: "idx_catalog_item_status_code")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model catalog_probability {
  catalog_probability_id BigInt   @id @default(autoincrement())
  code                   String   @unique @db.VarChar(50)
  name                   String   @db.VarChar(100)
  description            String?
  ordinal                Int      @unique
  numeric_value          Decimal? @db.Decimal(10, 4)
  is_active              Boolean  @default(true)
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  updated_at             DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

model domain_category {
  id          BigInt   @id(map: "process_category_pkey") @default(autoincrement())
  name        String   @db.VarChar(100)
  description String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamp(6)
  updated_at  DateTime @default(now()) @db.Timestamp(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model domain_dependency_matrix {
  from_domain_id   String   @db.Uuid
  to_domain_id     String   @db.Uuid
  influence_weight Decimal  @db.Decimal(6, 4)
  effect_type      String   @default("FRAGILITY_PROPAGATION")
  rationale        String?
  is_active        Boolean  @default(true)
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@ignore
  @@schema("core")
}

model frequency {
  id          String  @id @default(dbgenerated("core.gen_random_uuid()")) @db.Uuid
  title       String  @unique @db.VarChar(120)
  description String?

  @@schema("core")
}

model main_question {
  idquestion           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  question_no          Int      @unique(map: "uq_main_question_question_no") @db.SmallInt
  code                 String   @unique
  question             String
  description          String?
  objective            String?
  category             String
  analytical_dimension String?
  source_of_truth      String
  source_kind          String   @default("view")
  interpretation_rule  String?
  decision_use         String?
  business_meaning     String?
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  updated_at           DateTime @default(now()) @db.Timestamptz(6)
  graph_design         Json?

  @@index([category], map: "idx_main_question_category")
  @@index([code], map: "idx_main_question_code")
  @@index([source_kind], map: "idx_main_question_source_kind")
  @@schema("core")
}

model map_company_x_reino {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id String   @db.Uuid
  reino_id   String   @db.Uuid
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([company_id, reino_id], map: "uq_map_company_x_reino_company_reino_active", where: raw("(is_active = true)"))
  @@index([company_id], map: "idx_map_company_x_reino_company")
  @@index([is_active], map: "idx_map_company_x_reino_is_active")
  @@index([reino_id], map: "idx_map_company_x_reino_reino")
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_domain_element {
  id         String   @db.Uuid
  map_code   String
  domain_id  String   @db.Uuid
  element_id String   @db.Uuid
  is_primary Boolean  @default(true)
  rationale  Json?
  created_at DateTime @default(now()) @db.Timestamp(6)
  updated_at DateTime @default(now()) @db.Timestamp(6)

  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_framework_reino_jurisdiction {
  map_id               String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  framework_version_id String   @db.Uuid
  reino_id             String   @db.Uuid
  jurisdiction_id      String   @db.Uuid
  map_code             String?
  is_primary           Boolean? @default(false)
  rationale            Json?
  created_at           DateTime @default(now()) @db.Timestamp(6)
  updated_at           DateTime @default(now()) @db.Timestamp(6)
  relation_type        String

  @@ignore
  @@schema("core")
}

model map_lineal_risk_risk_control {
  map_lineal_risk_risk_control_id String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  catalog_lineal_risk_id          String   @db.Uuid
  catalog_lineal_control_id       String   @db.Uuid
  is_active                       Boolean  @default(true)
  created_at                      DateTime @default(now()) @db.Timestamptz(6)
  updated_at                      DateTime @default(now()) @db.Timestamptz(6)

  @@unique([catalog_lineal_risk_id, catalog_lineal_control_id], map: "uq_map_lineal_risk_risk_control")
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model map_reino_domain {
  id            String   @db.Uuid
  map_code      String
  reino_id      String   @db.Uuid
  domain_id     String   @db.Uuid
  link_strength Decimal? @db.Decimal(5, 4)
  rationale     Json?
  created_at    DateTime @default(now()) @db.Timestamp(6)
  updated_at    DateTime @default(now()) @db.Timestamp(6)

  @@ignore
  @@schema("core")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model model_run_obligation {
  id             String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_id         String    @db.Uuid
  obligation_id  String    @db.Uuid
  exposure_value Decimal?  @db.Decimal
  domain_code    String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@ignore
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model operational_risk_loss_event_type {
  id          BigInt   @id @default(autoincrement())
  code        String   @unique @db.VarChar(80)
  name        String   @db.VarChar(150)
  description String
  sort_order  Int      @default(0)
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model risk_appetite_catalog {
  risk_appetite_catalog_id BigInt   @id @default(autoincrement())
  appetite_code            String   @unique @db.VarChar(50)
  appetite_name            String   @db.VarChar(150)
  appetite_description     String?
  rationale                String?
  appetite_level           Int
  min_score                Int
  max_score                Int
  sequence_order           Int
  is_active                Boolean  @default(true)
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  updated_at               DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft {
  risk_assessment_draft_id  BigInt    @id @default(autoincrement())
  assessment_code           String    @unique @db.VarChar(100)
  title                     String    @db.VarChar(250)
  company_id                BigInt
  assessment_period_label   String?   @db.VarChar(100)
  scope_description         String?
  business_context          String?
  model_of_business         String?
  methodology_version       String?   @db.VarChar(50)
  status                    String    @default("draft") @db.VarChar(50)
  owner_manager_id          BigInt?
  submitted_for_approval_at DateTime? @db.Timestamptz(6)
  approved_at               DateTime? @db.Timestamptz(6)
  approved_by_manager_id    BigInt?
  concluded_at              DateTime? @db.Timestamptz(6)
  notes                     String?
  root_assessment_code      String    @db.VarChar(100)
  version_no                Int       @default(1)
  previous_draft_id         BigInt?
  is_current_version        Boolean   @default(true)
  locked_at                 DateTime? @db.Timestamptz(6)
  locked_by_manager_id      BigInt?
  created_at                DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id     BigInt?
  updated_by_manager_id     BigInt?
  is_deleted                Boolean   @default(false)
  deleted_at                DateTime? @db.Timestamptz(6)
  deleted_by_manager_id     BigInt?

  @@index([company_id], map: "idx_risk_assessment_draft_company")
  @@index([owner_manager_id], map: "idx_risk_assessment_draft_owner")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft_item {
  risk_assessment_draft_item_id BigInt    @id @default(autoincrement())
  risk_assessment_draft_id      BigInt
  significant_activity_id       String    @db.Uuid
  materiality_level             String    @db.VarChar(100)
  materiality_weight            Decimal?  @db.Decimal(12, 4)
  materiality_justification     String?
  sort_order                    Int       @default(1)
  notes                         String?
  created_at                    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id         BigInt?
  updated_by_manager_id         BigInt?
  is_deleted                    Boolean   @default(false)
  deleted_at                    DateTime? @db.Timestamptz(6)
  deleted_by_manager_id         BigInt?

  @@unique([risk_assessment_draft_id, significant_activity_id], map: "uq_risk_assessment_draft_item")
  @@index([risk_assessment_draft_id], map: "idx_risk_assessment_draft_item_draft")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft_item_action {
  risk_assessment_draft_item_action_id BigInt    @id @default(autoincrement())
  risk_assessment_draft_item_id        BigInt
  related_finding_id                   BigInt?
  action_title                         String    @db.VarChar(250)
  action_description                   String?
  owner_manager_id                     BigInt?
  target_date                          DateTime? @db.Date
  status                               String    @default("open") @db.VarChar(50)
  progress_notes                       String?
  created_at                           DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                           DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id                BigInt?
  updated_by_manager_id                BigInt?

  @@index([risk_assessment_draft_item_id], map: "idx_risk_assessment_draft_item_action_item")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft_item_control {
  risk_assessment_draft_item_control_id BigInt   @id @default(autoincrement())
  risk_assessment_draft_item_id         BigInt
  evaluation_scope                      String   @db.VarChar(50)
  control_name                          String   @db.VarChar(200)
  control_description                   String?
  effectiveness_level                   String   @db.VarChar(50)
  effectiveness_trend                   String?  @db.VarChar(50)
  net_risk_score                        Decimal? @db.Decimal(12, 4)
  net_risk_level                        String?  @db.VarChar(100)
  rationale                             String?
  created_at                            DateTime @default(now()) @db.Timestamptz(6)
  updated_at                            DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id                 BigInt?
  updated_by_manager_id                 BigInt?

  @@index([risk_assessment_draft_item_id], map: "idx_risk_assessment_draft_item_control_item")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft_item_finding {
  risk_assessment_draft_item_finding_id BigInt   @id @default(autoincrement())
  risk_assessment_draft_item_id         BigInt
  finding_type                          String   @db.VarChar(50)
  title                                 String   @db.VarChar(250)
  description                           String
  severity_level                        String?  @db.VarChar(50)
  recommendation                        String?
  created_at                            DateTime @default(now()) @db.Timestamptz(6)
  updated_at                            DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id                 BigInt?
  updated_by_manager_id                 BigInt?

  @@index([risk_assessment_draft_item_id], map: "idx_risk_assessment_draft_item_finding_item")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_draft_item_risk {
  risk_assessment_draft_item_risk_id BigInt   @id @default(autoincrement())
  risk_assessment_draft_item_id      BigInt
  risk_catalog_id                    String   @db.Uuid
  catalog_probability_id             BigInt
  catalog_impact_id                  BigInt
  inherent_risk_score                Decimal? @db.Decimal(12, 4)
  inherent_risk_level                String   @db.VarChar(100)
  inherent_risk_trend                String?  @db.VarChar(50)
  rationale                          String?
  created_at                         DateTime @default(now()) @db.Timestamptz(6)
  updated_at                         DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id              BigInt?
  updated_by_manager_id              BigInt?

  @@unique([risk_assessment_draft_item_id, risk_catalog_id], map: "uq_risk_assessment_draft_item_risk")
  @@index([risk_assessment_draft_item_id], map: "idx_risk_assessment_draft_item_risk_item")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final {
  risk_assessment_final_id BigInt    @id @default(autoincrement())
  source_draft_id          BigInt?
  assessment_code          String    @unique @db.VarChar(100)
  title                    String    @db.VarChar(250)
  company_id               BigInt
  assessment_period_label  String?   @db.VarChar(100)
  scope_description        String?
  business_context         String?
  model_of_business        String?
  methodology_version      String?   @db.VarChar(50)
  root_assessment_code     String    @db.VarChar(100)
  version_no               Int       @default(1)
  is_current_version       Boolean   @default(true)
  owner_manager_id         BigInt?
  approved_by_manager_id   BigInt?
  concluded_at             DateTime  @default(now()) @db.Timestamptz(6)
  global_net_risk_score    Decimal?  @db.Decimal(12, 4)
  global_net_risk_level    String?   @db.VarChar(100)
  global_net_risk_trend    String?   @db.VarChar(50)
  composite_risk_score     Decimal?  @db.Decimal(12, 4)
  composite_risk_level     String?   @db.VarChar(100)
  executive_summary        String?
  draft_snapshot_hash      String?   @db.VarChar(128)
  created_at               DateTime  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id    BigInt?
  updated_by_manager_id    BigInt?
  is_deleted               Boolean   @default(false)
  deleted_at               DateTime? @db.Timestamptz(6)
  deleted_by_manager_id    BigInt?

  @@index([company_id], map: "idx_risk_assessment_final_company")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final_item {
  risk_assessment_final_item_id BigInt    @id @default(autoincrement())
  risk_assessment_final_id      BigInt
  source_draft_item_id          BigInt?
  significant_activity_id       String?   @db.Uuid
  activity_code                 String    @db.VarChar(100)
  activity_name                 String    @db.VarChar(250)
  activity_description          String?
  materiality_level             String    @db.VarChar(100)
  materiality_weight            Decimal?  @db.Decimal(12, 4)
  materiality_justification     String?
  sort_order                    Int       @default(1)
  notes                         String?
  created_at                    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id         BigInt?
  updated_by_manager_id         BigInt?
  is_deleted                    Boolean   @default(false)
  deleted_at                    DateTime? @db.Timestamptz(6)
  deleted_by_manager_id         BigInt?

  @@index([risk_assessment_final_id], map: "idx_risk_assessment_final_item_final")
  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final_item_action {
  risk_assessment_final_item_action_id BigInt    @id @default(autoincrement())
  risk_assessment_final_item_id        BigInt
  source_draft_item_action_id          BigInt?
  related_finding_id                   BigInt?
  action_title                         String    @db.VarChar(250)
  action_description                   String?
  owner_manager_id                     BigInt?
  target_date                          DateTime? @db.Date
  status                               String    @db.VarChar(50)
  progress_notes                       String?
  created_at                           DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                           DateTime  @default(now()) @db.Timestamptz(6)
  created_by_manager_id                BigInt?
  updated_by_manager_id                BigInt?

  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final_item_control {
  risk_assessment_final_item_control_id BigInt   @id @default(autoincrement())
  risk_assessment_final_item_id         BigInt
  source_draft_item_control_id          BigInt?
  evaluation_scope                      String   @db.VarChar(50)
  control_name                          String   @db.VarChar(200)
  control_description                   String?
  effectiveness_level                   String   @db.VarChar(50)
  effectiveness_trend                   String?  @db.VarChar(50)
  net_risk_score                        Decimal? @db.Decimal(12, 4)
  net_risk_level                        String?  @db.VarChar(100)
  rationale                             String?
  created_at                            DateTime @default(now()) @db.Timestamptz(6)
  updated_at                            DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id                 BigInt?
  updated_by_manager_id                 BigInt?

  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final_item_finding {
  risk_assessment_final_item_finding_id BigInt   @id @default(autoincrement())
  risk_assessment_final_item_id         BigInt
  source_draft_item_finding_id          BigInt?
  finding_type                          String   @db.VarChar(50)
  title                                 String   @db.VarChar(250)
  description                           String
  severity_level                        String?  @db.VarChar(50)
  recommendation                        String?
  created_at                            DateTime @default(now()) @db.Timestamptz(6)
  updated_at                            DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id                 BigInt?
  updated_by_manager_id                 BigInt?

  @@schema("core")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_final_item_risk {
  risk_assessment_final_item_risk_id BigInt   @id @default(autoincrement())
  risk_assessment_final_item_id      BigInt
  source_draft_item_risk_id          BigInt?
  risk_catalog_id                    String?  @db.Uuid
  risk_code                          String   @db.VarChar(100)
  risk_name                          String   @db.VarChar(250)
  risk_description                   String?
  catalog_probability_id             BigInt?
  catalog_impact_id                  BigInt?
  inherent_risk_score                Decimal? @db.Decimal(12, 4)
  inherent_risk_level                String   @db.VarChar(100)
  inherent_risk_trend                String?  @db.VarChar(50)
  rationale                          String?
  created_at                         DateTime @default(now()) @db.Timestamptz(6)
  updated_at                         DateTime @default(now()) @db.Timestamptz(6)
  created_by_manager_id              BigInt?
  updated_by_manager_id              BigInt?

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_assessment_report {
  risk_assessment_report_id BigInt    @id @default(autoincrement())
  risk_assessment_final_id  BigInt
  report_code               String    @unique @db.VarChar(100)
  report_type               String    @default("final_report") @db.VarChar(50)
  report_title              String    @db.VarChar(250)
  matrix_summary            String?
  executive_summary         String?
  report_body               String?
  issued_at                 DateTime? @db.Timestamptz(6)
  issued_by_manager_id      BigInt?
  created_at                DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                DateTime  @default(now()) @db.Timestamptz(6)

  @@index([risk_assessment_final_id], map: "idx_risk_assessment_report_final")
  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_emerging_source {
  id          BigInt   @id @default(autoincrement())
  code        String   @unique @db.VarChar(50)
  name        String   @db.VarChar(100)
  description String?
  sort_order  Int      @default(0)
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_emerging_status {
  id          BigInt   @id @default(autoincrement())
  code        String   @unique @db.VarChar(30)
  name        String   @db.VarChar(50)
  description String?
  sort_order  Int      @default(0)
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model risk_factor {
  id          BigInt   @id @default(autoincrement())
  code        String   @unique @db.VarChar(50)
  name        String   @db.VarChar(100)
  description String
  sort_order  Int      @default(0)
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("core")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model risk_scale {
  id             BigInt    @id @default(autoincrement())
  code           String    @db.VarChar(50)
  name           String    @db.VarChar(100)
  description    String?
  min_value      Decimal   @db.Decimal(10, 2)
  max_value      Decimal   @db.Decimal(10, 2)
  severity_rank  Int
  color_hex      String?   @db.VarChar(7)
  is_active      Boolean   @default(true)
  applies_to     String    @default("ALL") @db.VarChar(30)
  version        Int       @default(1)
  effective_from DateTime? @db.Timestamp(6)
  effective_to   DateTime? @db.Timestamp(6)
  created_at     DateTime  @default(now()) @db.Timestamp(6)
  updated_at     DateTime  @default(now()) @db.Timestamp(6)

  @@unique([code, applies_to, version], map: "uq_risk_scale_band")
  @@schema("core")
}

model map_users_x_roles {
  id             String         @id(map: "map_users_x_roles_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String         @db.Uuid
  role_id        String         @db.Uuid
  is_active      Boolean        @default(true)
  created_at     DateTime       @default(now()) @db.Timestamp(6)
  updated_at     DateTime       @default(now()) @db.Timestamp(6)
  security_users security_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_map_users_x_roles_user")

  @@map("_map_users_x_roles")
  @@schema("security")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_model_run_control {
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_id              String    @db.Uuid
  control_id          String    @db.Uuid
  exists_score        Decimal?  @db.Decimal
  design_score        Decimal?  @db.Decimal
  formalization_score Decimal?  @db.Decimal
  operating_score     Decimal?  @db.Decimal
  test_score          Decimal?  @db.Decimal
  evidence_score      Decimal?  @db.Decimal
  effectiveness       Decimal?  @db.Decimal
  created_at          DateTime? @default(now()) @db.Timestamptz(6)

  @@map("__borrame_model_run_control")
  @@ignore
  @@schema("z_borrame")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_risk_manager {
  risk_manager_id BigInt   @id(map: "risk_manager_pkey") @default(autoincrement())
  full_name       String   @db.VarChar(200)
  email           String?  @unique(map: "risk_manager_email_key") @db.VarChar(255)
  position_title  String?  @db.VarChar(150)
  area_name       String?  @db.VarChar(150)
  is_active       Boolean  @default(true)
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  updated_at      DateTime @default(now()) @db.Timestamptz(6)

  @@map("_borrame-risk_manager")
  @@schema("z_borrame")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_significant_activity {
  company_id              String   @db.Uuid
  activity_code           String   @db.VarChar(100)
  activity_name           String   @db.VarChar(250)
  activity_description    String?
  is_active               Boolean  @default(true)
  created_at              DateTime @default(now()) @db.Timestamptz(6)
  updated_at              DateTime @default(now()) @db.Timestamptz(6)
  significant_activity_id String   @id(map: "significant_activity_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  responsible             String?
  frequency               String?
  risk_weight             Decimal? @db.Decimal
  cascade_factor          Decimal? @db.Decimal
  is_cascade              Boolean? @default(false)

  @@unique([company_id, activity_code], map: "uq_significant_activity_company_code")
  @@map("_borrame-significant_activity")
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit {
  id                        String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String    @db.Uuid
  company_id                String    @db.Uuid
  version_no                Int       @default(1)
  supersedes_audit_id       String?   @db.Uuid
  name                      String
  objective                 String?
  scope_notes               String?
  period_start              DateTime? @db.Date
  period_end                DateTime? @db.Date
  status_code               String    @default("closed")
  lead_auditor_user_id      String?   @db.Uuid
  lead_auditor_display_name String?
  promoted_from_draft_id    String    @db.Uuid
  promotion_batch_id        String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  promoted_at               DateTime  @default(now()) @db.Timestamptz(6)
  promoted_by               String?   @db.Uuid
  immutable_lock            Boolean   @default(true)

  @@map("_borrame_audit_audit")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit_draft {
  id                        String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String    @db.Uuid
  company_id                String    @db.Uuid
  name                      String
  objective                 String?
  scope_notes               String?
  period_start              DateTime? @db.Date
  period_end                DateTime? @db.Date
  status_code               String    @default("draft")
  submitted_at              DateTime? @db.Timestamptz(6)
  audited_responded_at      DateTime? @db.Timestamptz(6)
  closed_at                 DateTime? @db.Timestamptz(6)
  lead_auditor_user_id      String?   @db.Uuid
  lead_auditor_display_name String?
  created_at                DateTime  @default(now()) @db.Timestamptz(6)
  created_by                String?   @db.Uuid
  updated_at                DateTime  @default(now()) @db.Timestamptz(6)
  updated_by                String?   @db.Uuid

  @@map("_borrame_audit_audit_draft")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit_expert_usage {
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id            String    @db.Uuid
  supported_user_id   String    @db.Uuid
  expert_user_id      String?   @db.Uuid
  expert_display_name String
  expert_org          String?
  expertise_area      String?
  engagement_notes    String?
  start_at            DateTime? @db.Timestamptz(6)
  end_at              DateTime? @db.Timestamptz(6)
  created_at          DateTime  @db.Timestamptz(6)
  created_by          String?   @db.Uuid

  @@map("_borrame_audit_audit_expert_usage")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit_expert_usage_draft {
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id            String    @db.Uuid
  supported_user_id   String    @db.Uuid
  expert_user_id      String?   @db.Uuid
  expert_display_name String
  expert_org          String?
  expertise_area      String?
  engagement_notes    String?
  start_at            DateTime? @db.Timestamptz(6)
  end_at              DateTime? @db.Timestamptz(6)
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  created_by          String?   @db.Uuid

  @@map("_borrame_audit_audit_expert_usage_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit_team {
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id       String   @db.Uuid
  user_id        String?  @db.Uuid
  display_name   String
  email          String?
  team_role_code String
  is_external    Boolean  @default(false)
  expertise_area String?
  added_at       DateTime @db.Timestamptz(6)
  added_by       String?  @db.Uuid

  @@map("_borrame_audit_audit_team")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audit_team_draft {
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id       String   @db.Uuid
  user_id        String?  @db.Uuid
  display_name   String
  email          String?
  team_role_code String
  is_external    Boolean  @default(false)
  expertise_area String?
  added_at       DateTime @default(now()) @db.Timestamptz(6)
  added_by       String?  @db.Uuid

  @@map("_borrame_audit_audit_team_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audited_response {
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  finding_id          String    @db.Uuid
  response_text       String
  agrees              Boolean?
  root_cause_ack      String?
  action_plan_summary String?
  target_date         DateTime? @db.Date
  owner_user_id       String?   @db.Uuid
  created_at          DateTime  @db.Timestamptz(6)
  created_by          String?   @db.Uuid
  updated_at          DateTime  @db.Timestamptz(6)
  updated_by          String?   @db.Uuid

  @@map("_borrame_audit_audited_response")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_audited_response_draft {
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  finding_id          String    @db.Uuid
  response_text       String
  agrees              Boolean?
  root_cause_ack      String?
  action_plan_summary String?
  target_date         DateTime? @db.Date
  owner_user_id       String?   @db.Uuid
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  created_by          String?   @db.Uuid
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_by          String?   @db.Uuid

  @@map("_borrame_audit_audited_response_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_auditor_addendum {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id             String   @db.Uuid
  type                 String
  content              String
  auditor_user_id      String   @db.Uuid
  auditor_display_name String
  created_at           DateTime @db.Timestamptz(6)
  created_by           String?  @db.Uuid

  @@map("_borrame_audit_auditor_addendum")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_auditor_addendum_draft {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id             String   @db.Uuid
  type                 String
  content              String
  auditor_user_id      String   @db.Uuid
  auditor_display_name String
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  created_by           String?  @db.Uuid

  @@map("_borrame_audit_auditor_addendum_draft")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_catalog_finding_severity {
  code        String
  rank        Int    @db.SmallInt
  description String

  @@map("_borrame_audit_catalog_finding_severity")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_catalog_report_type {
  code        String
  description String

  @@map("_borrame_audit_catalog_report_type")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_catalog_status {
  code        String
  description String

  @@map("_borrame_audit_catalog_status")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_catalog_storage_provider {
  code        String
  description String

  @@map("_borrame_audit_catalog_storage_provider")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_catalog_team_role {
  code        String
  description String

  @@map("_borrame_audit_catalog_team_role")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_closure {
  id            String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id      String   @db.Uuid
  closure_notes String?
  closed_at     DateTime @db.Timestamptz(6)
  closed_by     String?  @db.Uuid

  @@map("_borrame_audit_closure")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_closure_draft {
  id            String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id      String    @db.Uuid
  closure_notes String?
  closed_at     DateTime? @db.Timestamptz(6)
  closed_by     String?   @db.Uuid

  @@map("_borrame_audit_closure_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_corpus_selection {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id             String   @db.Uuid
  jurisdiction_id      String?  @db.Uuid
  framework_id         String?  @db.Uuid
  framework_version_id String   @db.Uuid
  corpus_snapshot_hash String?
  selection_notes      String?
  created_at           DateTime @db.Timestamptz(6)
  created_by           String?  @db.Uuid

  @@map("_borrame_audit_corpus_selection")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_corpus_selection_draft {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id             String   @db.Uuid
  jurisdiction_id      String?  @db.Uuid
  framework_id         String?  @db.Uuid
  framework_version_id String   @db.Uuid
  corpus_snapshot_hash String?
  selection_notes      String?
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  created_by           String?  @db.Uuid

  @@map("_borrame_audit_corpus_selection_draft")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_evaluation {
  id                      String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id                String   @db.Uuid
  domain_id               String?  @db.Uuid
  obligation_id           String?  @db.Uuid
  control_id              String?  @db.Uuid
  assessment_method       String?
  design_effectiveness    Decimal? @db.Decimal(5, 2)
  operating_effectiveness Decimal? @db.Decimal(5, 2)
  notes                   String?
  created_at              DateTime @db.Timestamptz(6)
  created_by              String?  @db.Uuid
  updated_at              DateTime @db.Timestamptz(6)
  updated_by              String?  @db.Uuid

  @@map("_borrame_audit_evaluation")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_evaluation_draft {
  id                      String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id                String   @db.Uuid
  domain_id               String?  @db.Uuid
  obligation_id           String?  @db.Uuid
  control_id              String?  @db.Uuid
  assessment_method       String?
  design_effectiveness    Decimal? @db.Decimal(5, 2)
  operating_effectiveness Decimal? @db.Decimal(5, 2)
  notes                   String?
  created_at              DateTime @default(now()) @db.Timestamptz(6)
  created_by              String?  @db.Uuid
  updated_at              DateTime @default(now()) @db.Timestamptz(6)
  updated_by              String?  @db.Uuid

  @@map("_borrame_audit_evaluation_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_event_log {
  id            BigInt   @default(autoincrement())
  tenant_id     String   @db.Uuid
  entity_name   String
  entity_id     String   @db.Uuid
  event_type    String
  actor_user_id String?  @db.Uuid
  payload       Json?
  created_at    DateTime @default(now()) @db.Timestamptz(6)

  @@map("_borrame_audit_event_log")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_finding {
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id       String   @db.Uuid
  evaluation_id  String?  @db.Uuid
  title          String
  description    String
  criteria       String?
  condition      String?
  cause          String?
  effect         String?
  severity_code  String
  risk_statement String?
  recommendation String?
  is_material    Boolean  @default(false)
  created_at     DateTime @db.Timestamptz(6)
  created_by     String?  @db.Uuid
  updated_at     DateTime @db.Timestamptz(6)
  updated_by     String?  @db.Uuid

  @@map("_borrame_audit_finding")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_finding_draft {
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id       String   @db.Uuid
  evaluation_id  String?  @db.Uuid
  title          String
  description    String
  criteria       String?
  condition      String?
  cause          String?
  effect         String?
  severity_code  String
  risk_statement String?
  recommendation String?
  is_material    Boolean  @default(false)
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  created_by     String?  @db.Uuid
  updated_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_by     String?  @db.Uuid

  @@map("_borrame_audit_finding_draft")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_finding_evidence {
  id                    String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  finding_id            String   @db.Uuid
  storage_provider_code String
  bucket                String?
  object_key            String
  logical_path          String?
  sha256                String
  file_name_original    String
  mime_type             String?
  size_bytes            BigInt?
  uploaded_at           DateTime @db.Timestamptz(6)
  uploaded_by           String?  @db.Uuid
  caption               String?
  is_sealed             Boolean  @default(true)

  @@map("_borrame_audit_finding_evidence")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_finding_evidence_draft {
  id                    String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  finding_id            String   @db.Uuid
  storage_provider_code String
  bucket                String?
  object_key            String
  logical_path          String?
  sha256                String
  file_name_original    String
  mime_type             String?
  size_bytes            BigInt?
  uploaded_at           DateTime @default(now()) @db.Timestamptz(6)
  uploaded_by           String?  @db.Uuid
  caption               String?
  is_sealed             Boolean  @default(false)

  @@map("_borrame_audit_finding_evidence_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_report {
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id           String    @db.Uuid
  report_type_code   String
  executive_summary  String?
  methodology        String?
  overall_conclusion String?
  generated_at       DateTime? @db.Timestamptz(6)
  generated_by       String?   @db.Uuid
  content_json       Json?
  created_at         DateTime  @db.Timestamptz(6)
  created_by         String?   @db.Uuid
  updated_at         DateTime  @db.Timestamptz(6)
  updated_by         String?   @db.Uuid

  @@map("_borrame_audit_report")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_audit_report_draft {
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  audit_id           String    @db.Uuid
  report_type_code   String
  executive_summary  String?
  methodology        String?
  overall_conclusion String?
  generated_at       DateTime? @db.Timestamptz(6)
  generated_by       String?   @db.Uuid
  content_json       Json?
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  created_by         String?   @db.Uuid
  updated_at         DateTime  @default(now()) @db.Timestamptz(6)
  updated_by         String?   @db.Uuid

  @@map("_borrame_audit_report_draft")
  @@ignore
  @@schema("z_borrame")
}

model borrame_catalog_group {
  id          BigInt   @id(map: "catalog_group_pkey")
  code        String?  @db.VarChar
  name        String?  @db.VarChar
  description String?
  applies_to  String?  @db.VarChar
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("_borrame_catalog_group")
  @@schema("z_borrame")
}

model borrame_company {
  id               String                     @id(map: "company_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code             String                     @unique(map: "company_code_key")
  name             String
  is_active        Boolean                    @default(true)
  created_at       DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime                   @default(now()) @db.Timestamptz(6)
  legal_name       String?
  jurisdiction_id  String?                    @db.Uuid
  description      String?
  company_licenses security_company_license[]
  users            security_users[]

  @@map("_borrame_company")
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model borrame_company_ontology {
  id          String   @id(map: "company_ontology_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code        String   @unique(map: "company_ontology_code_key")
  name        String
  description String
  selection   Json
  is_active   Boolean  @default(true)
  sort_order  Int      @default(0)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@index([is_active], map: "idx_company_ontology_is_active")
  @@index([name], map: "idx_company_ontology_name")
  @@index([sort_order], map: "idx_company_ontology_sort_order")
  @@map("_borrame_company_ontology")
  @@schema("z_borrame")
}

model borrame_company_ontology_assignment {
  company_id  String   @id(map: "company_ontology_assignment_pkey") @db.Uuid
  ontology_id String   @db.Uuid
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_by  String?  @db.Uuid

  @@index([is_active], map: "idx_company_ontology_assignment_active")
  @@index([ontology_id], map: "idx_company_ontology_assignment_ontology")
  @@map("_borrame_company_ontology_assignment")
  @@schema("z_borrame")
}

model borrame_control_catalog {
  control_id  String   @id(map: "control_catalog_pkey") @unique(map: "catalog_lineal_control_control_id_key") @db.Uuid
  name        String
  description String?
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)

  @@map("_borrame_control_catalog")
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model borrame_control_company {
  id           String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id   String    @db.Uuid
  name         String
  description  String?
  control_type String?
  nature       String?
  owner        String?
  frequency    String?
  is_active    Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@map("_borrame_control_company")
  @@ignore
  @@schema("z_borrame")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_framework_version {
  id             String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  framework_id   String    @db.Uuid
  version        String
  effective_date DateTime? @db.Date
  changelog      String?
  status         String    @default("active")
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)
  status_id      Int?      @db.SmallInt
  scope          String?

  @@map("_borrame_framework_version")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_jurisdiction {
  id         String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code       String
  name       String
  iso2       String?
  iso3       String?
  status     Boolean  @default(false)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  status_id  Int?     @db.SmallInt

  @@map("_borrame_jurisdiction")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_model_run {
  id                  String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_timestamp       DateTime @default(now()) @db.Timestamptz(6)
  profile_snapshot_id BigInt
  corpus_version      String
  exposure_score      Decimal? @db.Decimal
  readiness_score     Decimal? @db.Decimal
  exposure_raw        Decimal? @db.Decimal
  trigger_applied     Boolean?
  created_by          String?  @db.Uuid

  @@map("_borrame_model_run")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_model_run_risk {
  id               String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_id           String    @db.Uuid
  risk_id          String    @db.Uuid
  exposure_value   Decimal?  @db.Decimal
  mitigation_value Decimal?  @db.Decimal
  created_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@map("_borrame_model_run_risk")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_model_run_trigger {
  id            String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_id        String    @db.Uuid
  trigger_code  String
  trigger_floor Decimal?  @db.Decimal
  created_at    DateTime? @default(now()) @db.Timestamptz(6)

  @@map("_borrame_model_run_trigger")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_obligation_graph {
  id                     String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  parent_obligation_code String    @db.VarChar(50)
  child_obligation_code  String    @db.VarChar(50)
  dependency_type        String    @db.VarChar(50)
  dependency_strength    Decimal   @db.Decimal(4, 2)
  collapse_trigger       Boolean?  @default(false)
  propagation_multiplier Decimal?  @default(1.0) @db.Decimal(4, 2)
  created_at             DateTime? @default(now()) @db.Timestamp(6)
  updated_at             DateTime? @default(now()) @db.Timestamp(6)
  rationale              Json?
  review_priority        Int?      @db.SmallInt
  is_active              Boolean   @default(true)

  @@map("_borrame_obligation_graph")
  @@ignore
  @@schema("z_borrame")
}

model borrame_reino {
  id          String   @unique(map: "reino_id_unique") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code        String
  name        String
  description String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamp(6)
  updated_at  DateTime @default(now()) @db.Timestamp(6)

  @@map("_borrame_reino")
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_control {
  run_id     String   @db.Uuid
  control_id String   @db.Uuid
  score      Decimal? @db.Decimal(10, 6)
  rank       Int?
  reasons    Json?

  @@map("_borrame_run_control")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_control_draft {
  run_id     String   @db.Uuid
  control_id String   @db.Uuid
  score      Decimal? @db.Decimal(10, 6)
  rank       Int?
  reasons    Json?

  @@map("_borrame_run_control_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_draft {
  id                   String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id           String   @db.Uuid
  framework_version_id String   @db.Uuid
  period_start         DateTime @db.Date
  period_end           DateTime @db.Date
  mode                 String
  status               String   @default("draft")
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  updated_at           DateTime @default(now()) @db.Timestamptz(6)

  @@map("_borrame_run_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_obligation {
  run_id        String   @db.Uuid
  obligation_id String   @db.Uuid
  score         Decimal? @db.Decimal(10, 6)
  rank          Int?
  reasons       Json?

  @@map("_borrame_run_obligation")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_obligation_draft {
  run_id        String   @db.Uuid
  obligation_id String   @db.Uuid
  score         Decimal? @db.Decimal(10, 6)
  rank          Int?
  reasons       Json?

  @@map("_borrame_run_obligation_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_risk {
  run_id  String   @db.Uuid
  risk_id String   @db.Uuid
  score   Decimal? @db.Decimal(10, 6)
  rank    Int?
  reasons Json?

  @@map("_borrame_run_risk")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_risk_draft {
  run_id  String   @db.Uuid
  risk_id String   @db.Uuid
  score   Decimal? @db.Decimal(10, 6)
  rank    Int?
  reasons Json?

  @@map("_borrame_run_risk_draft")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_test {
  run_id  String   @db.Uuid
  test_id String   @db.Uuid
  score   Decimal? @db.Decimal(10, 6)
  rank    Int?
  reasons Json?

  @@map("_borrame_run_test")
  @@ignore
  @@schema("z_borrame")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model borrame_run_test_draft {
  run_id  String   @db.Uuid
  test_id String   @db.Uuid
  score   Decimal? @db.Decimal(10, 6)
  rank    Int?
  reasons Json?

  @@map("_borrame_run_test_draft")
  @@ignore
  @@schema("z_borrame")
}

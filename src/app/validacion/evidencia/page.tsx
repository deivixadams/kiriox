import EvidenceVault from "@/components/operativo/EvidenceVault";

export default function EvidencePage() {
    return (
        <div style={{ padding: '2rem' }}>
            <EvidenceVault />
        </div>
    );
}

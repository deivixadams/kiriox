import ScoreAssessmentDetailPage from '@/modules/linear-risk/ui/pages/ScoreAssessmentDetailPage';

export default function AssessmentDetailPage({ params }: { params: { id: string } }) {
  return <ScoreAssessmentDetailPage params={params} />;
}
